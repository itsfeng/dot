
# ---------------------------------
# bash verbose
set -ex

# ---------------------------------
# test if file exists
[[ -f /etc/resolv.conf ]] && echo "$FILE exist"

# ---------------------------------
# variables & braces
# variables ${var} vs "${var}" vs $var
[What is the difference between ${var}, "$var", and "${var}" in the Bash shell?](https://stackoverflow.com/questions/18135451/what-is-the-difference-between-var-var-and-var-in-the-bash-shell)

# ---------------------------------
# prepend a line in bash
echo -e "DATA-Line-1\n$(cat input)" > input

# ---------------------------------
# prepend multiple lines in bash
echo -e "DATA-Line-1\nDATA-Line-2\n$(cat input)" > input
